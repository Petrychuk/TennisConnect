node_modules/
dist/
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env.dev
.env-e 

# Environment and credential files
.env
.env.*
*.env
.env.sync
!.env.example
credentials.json
*.pem
*.key
.credentials

# Playwright
# Test-only credentials (dummy accounts, not real secrets) - kept out of
# the repo anyway so they don't show up in a public/shared clone. If this
# file is already tracked, adding it here alone won't remove it from git -
# also run: git rm --cached tests/fixtures/test-users.ts
tests/fixtures/test-users.ts
test-results/
playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
playwright/.auth/